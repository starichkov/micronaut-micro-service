datasources:
  default:
    url: jdbc:h2:mem:${database.schema:microshop};MODE=MYSQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: 'sa'
    password: ''
    driverClassName: org.h2.Driver

jpa:
  default:
    properties:
      hibernate:
        show_sql: true

logger:
  levels:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE